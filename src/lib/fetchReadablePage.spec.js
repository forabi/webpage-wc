describe('fetchReadablyPage', () => {
  it('should return a Promise');
  it('should fail on non 2xx response codes');
  it('resolves to an object with a property "body"');
  it('"body" should have "content" as a string');
  it('should reject with a "statusCode" on error object');
});
